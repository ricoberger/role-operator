apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "role-operator.fullname" . }}
  labels:
    {{- include "role-operator.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ tpl .Values.config . | indent 4 }}
